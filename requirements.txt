# Build dependencies (needed at compile time)
numpy>=1.23
Cython>=3.0
pybind11>=2.12.0

# PyTorch: install separately BEFORE running this file (via install_deps.sh).
# e.g. CUDA 12.x: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#      CUDA 11.3:  pip install torch==1.11.0+cu113 torchvision==0.12.0+cu113 --extra-index-url https://download.pytorch.org/whl/cu113
torch>=1.8
torchvision>=0.9
torchaudio>=0.8

# detectron2 core dependencies
fvcore>=0.1.5,<0.1.6
iopath>=0.1.7,<0.1.10
omegaconf>=2.2
hydra-core>=1.2
yacs>=0.1.8
cloudpickle
termcolor
tabulate
tqdm>4.29.0
tensorboard

# Research / experiment
imagecorruptions==1.1.2
pycocotools>=2.0.7
lvis==0.5.3
fairscale==0.4.11
timm==0.5.4
torch-kmeans==0.2.0
scikit-learn>=1.2.0
scikit-image>=0.25.0
scipy>=1.11.0

# Vision / data
opencv-python>=4.7.0
Pillow>=9.4.0
matplotlib>=3.8.0
pandas>=2.0.0
imageio>=2.33,!=2.35.0

# CLIP (installed separately via install_deps.sh, but keep its text deps)
ftfy==6.1.1
regex==2022.9.13

# Logging / experiment tracking
wandb>=0.15.0
psutil

# Misc utilities
PyYAML==6.0
addict==2.4.0
gdown>=4.7.1
portalocker>=2.5.1
packaging
yapf==0.32.0
pydot==1.4.2
h5py==3.9.0
